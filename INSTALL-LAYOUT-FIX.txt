Replace main.py at the top level of the Stat-Tracker repository. Keep the enclosed test files inside tests. Commit and push, wait for the web deployment to finish, then refresh Edge with Ctrl+Shift+R. This includes the previous saved-name startup fix. The lower workspace gets 60% of available app height; the upper header scrolls independently and tabs remain visible. No database changes are required. Existing native installers are unchanged; this patch updates the website after deployment.
